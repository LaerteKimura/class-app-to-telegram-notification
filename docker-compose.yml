services:
  classapp-notifier:
    build: .
    container_name: classapp-notifier
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro   # credentials — read-only
      - ./data:/data                         # session, state, log
